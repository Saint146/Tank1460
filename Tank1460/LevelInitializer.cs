using Microsoft.Xna.Framework;
using Tank1460;
using Tank1460.Common.Level.Object;
using Tank1460.Common.Level.Object.Tile;

public class LevelInitializer
{
    public static void LoadTiles(Level level, TileType[,] tileTypes)
    {
        // Move tile loading logic here
    }

    public static void LoadObjects(Level level, LevelObjectModel[] objectModels)
    {
        // Move object loading logic here
    }

    private static void CreatePlayerSpawn(Level level, Rectangle tileBounds, PlayerIndex playerIndex)
    {
        // Move player spawn creation here
    }

    private static void CreateFalcon(Level level, Rectangle tileBounds)
    {
        // Move falcon creation here
    }

    private static void CreateBotSpawn(Level level, Rectangle tileBounds)
    {
        // Move bot spawn creation here
    }
}